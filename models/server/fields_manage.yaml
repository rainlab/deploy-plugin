# ===================================
#  Form Field Definitions
# ===================================

fields:
    _tip1:
        type: hint
        mode: success
        label: Server is Ready to Deploy

    _action_deploy:
        type: partial
        path: action_deploy

    _action_update_config:
        type: partial
        path: action_update_config

    _action_run_shell:
        type: partial
        path: action_run_shell

    _action_view_key:
        type: partial
        path: action_view_key
